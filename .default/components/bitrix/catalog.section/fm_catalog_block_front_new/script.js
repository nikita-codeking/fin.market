$(document).ready(function(){

    function sayHi() {
        /*var sliderPosition = $(".owl-stage").css("transform");
        var comma = ',';
        splitString(sliderPosition, comma);
        function splitString(stringToSplit, separator) {
            var arrayOfStrings = stringToSplit.split(separator);
            arrayOfStrings[4] = Number(arrayOfStrings[4]) - 140;
            document.getElementsByClassName("owl-stage")[0].style.transition = 'all 1s 0.1s ease';
            document.getElementsByClassName("owl-stage")[0].style.transform = 'translate3d(' + arrayOfStrings[4] +'px, 0px, 0px)';
        }*/
    }

    //setTimeout(sayHi, 500);

});